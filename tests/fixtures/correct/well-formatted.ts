// This file is correctly formatted
export function greet(name: string): string {
  return `Hello, ${name}!`
}

export const config = {
  name: 'test',
  version: '1.0.0'
}

export function add(a: number, b: number): number {
  return a + b
}
